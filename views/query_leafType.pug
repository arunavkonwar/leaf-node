extends layout


block content
  h1 #{title}
  ul.list-group
    each article, i in articles
      if (article.single==NULL)
        li.list-group-item
          a(href="/articles/"+article._id)= i+1 + '. ' + article.title
  h1 #{i}
